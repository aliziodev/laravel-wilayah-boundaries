name: Sync Upstream Boundaries

on:
  schedule:
    - cron: '30 3 * * *'   # 03:30 UTC setiap hari (30 menit setelah wilayah utama)
  workflow_dispatch:
    inputs:
      force:
        description: 'Paksa sync walau hash upstream tidak berubah'
        required: false
        default: false
        type: boolean

jobs:
  sync:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.3'
          extensions: curl

      - name: Cek hash upstream (branch main)
        id: check
        env:
          FORCE_SYNC: ${{ github.event.inputs.force || 'false' }}
        run: |
          UPSTREAM_HASH=$(curl -sf \
            -H "Accept: application/vnd.github.v3+json" \
            -H "Authorization: Bearer ${{ secrets.GITHUB_TOKEN }}" \
            "https://api.github.com/repos/cahyadsn/wilayah_boundaries/commits/main" \
            | php -r '$data = json_decode(stream_get_contents(STDIN), true); echo $data["sha"] ?? "null";')

          CURRENT_HASH=$(php -r "
            \$v = @include 'data/version.php';
            echo \$v['source_hash'] ?? 'none';
          ")

          echo "upstream_hash=$UPSTREAM_HASH" >> $GITHUB_OUTPUT
          echo "current_hash=$CURRENT_HASH"   >> $GITHUB_OUTPUT

          if [ "$UPSTREAM_HASH" = "null" ]; then
            echo "Gagal membaca hash upstream dari GitHub API."
            exit 1
          fi

          if [ "$FORCE_SYNC" = "true" ]; then
            echo "changed=true"  >> $GITHUB_OUTPUT
            echo "mode=force"    >> $GITHUB_OUTPUT
            echo "🔁 Force sync aktif: menjalankan sinkronisasi walau hash sama."
          elif [ "$UPSTREAM_HASH" != "$CURRENT_HASH" ]; then
            echo "changed=true"  >> $GITHUB_OUTPUT
            echo "mode=hash"     >> $GITHUB_OUTPUT
            echo "📦 Boundaries berubah: $CURRENT_HASH → $UPSTREAM_HASH"
          else
            echo "changed=false" >> $GITHUB_OUTPUT
            echo "mode=skip"     >> $GITHUB_OUTPUT
            echo "✅ Boundaries sudah up-to-date."
          fi

      - name: Set release metadata
        if: steps.check.outputs.changed == 'true'
        id: meta
        run: |
          cp data/version.php /tmp/boundaries-version-before.php 2>/dev/null || true
          echo "release_date=$(date +%Y-%m-%d)" >> $GITHUB_OUTPUT

      - name: Jalankan Normalizer Boundaries
        if: steps.check.outputs.changed == 'true'
        env:
          UPSTREAM_HASH: ${{ steps.check.outputs.upstream_hash }}
        run: |
          # Fetch langsung dari raw URL — tanpa git clone
          php .github/scripts/normalize.php \
            "https://raw.githubusercontent.com/cahyadsn/wilayah_boundaries/refs/heads/main/db"

      - name: Bump versi package
        if: steps.check.outputs.changed == 'true'
        id: version
        run: |
          CURRENT=$(php -r "
            \$c = json_decode(file_get_contents('composer.json'), true);
            echo \$c['version'] ?? '1.0.0';
          ")
          IFS='.' read -r MAJOR MINOR PATCH <<< "$CURRENT"
          NEW_VERSION="$MAJOR.$MINOR.$((PATCH + 1))"
          echo "current_version=$CURRENT" >> $GITHUB_OUTPUT
          echo "new_version=$NEW_VERSION" >> $GITHUB_OUTPUT

          php -r "
            \$c = json_decode(file_get_contents('composer.json'), true);
            \$c['version'] = '$NEW_VERSION';
            file_put_contents('composer.json', json_encode(\$c, JSON_PRETTY_PRINT | JSON_UNESCAPED_SLASHES) . PHP_EOL);
          "

          mv .build/boundaries-dataset.ndjson.gz ".build/boundaries-dataset-v${NEW_VERSION}.ndjson.gz"

          php -r "
            \$manifest = include 'data/version.php';
            \$manifest['asset']['name'] = 'boundaries-dataset-v{$NEW_VERSION}.ndjson.gz';
            \$manifest['asset']['url'] = 'https://github.com/aliziodev/laravel-wilayah-boundaries/releases/download/v{$NEW_VERSION}/boundaries-dataset-v{$NEW_VERSION}.ndjson.gz';
            \$manifest['asset']['size_bytes'] = filesize('.build/boundaries-dataset-v{$NEW_VERSION}.ndjson.gz');
            file_put_contents('data/version.php', '<?php' . PHP_EOL . '// Auto-generated by CI/CD. DO NOT EDIT.' . PHP_EOL . 'return ' . var_export(\$manifest, true) . ';' . PHP_EOL);
          "

      - name: Generate changelog and release notes
        if: steps.check.outputs.changed == 'true'
        run: |
          php .github/scripts/generate_changelog.php \
            /tmp/boundaries-version-before.php \
            data/version.php \
            CHANGELOG.md \
            .github/release-notes.md \
            "${{ steps.version.outputs.current_version }}" \
            "${{ steps.version.outputs.new_version }}"

      - name: Commit & Push
        if: steps.check.outputs.changed == 'true'
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add -A data composer.json CHANGELOG.md
          git commit -m "data: sync boundaries $(date +%Y-%m-%d) [skip ci]

          Source: cahyadsn/wilayah_boundaries@${{ steps.check.outputs.upstream_hash }}"
          git push

      - name: Buat GitHub Release
        if: steps.check.outputs.changed == 'true'
        uses: softprops/action-gh-release@v1
        with:
          tag_name: v${{ steps.version.outputs.new_version }}
          name: "v${{ steps.version.outputs.new_version }} - Sync ${{ steps.meta.outputs.release_date }}"
          body_path: .github/release-notes.md
          files: .build/boundaries-dataset-v${{ steps.version.outputs.new_version }}.ndjson.gz
          generate_release_notes: false
